设计一个卷积码及其硬判决（Hard-decision）和软判决（Soft-decision）维特比（Viterbi）译码器。具体要求如下：编写卷积码编码器程序；编写硬判决和软判决维特比（Viterbi）译码器程序；对程序进行仿真，获取误码率（BER）随信道信噪比（SNR）变化的性能表现。（你可以使用 Excel 或 MATLAB 来生成图表。）*（可选） 编写一个 BCJR 译码器，并重复步骤 (3)。*（可选） 编写 Turbo 码的编码器和译码器，并重复步骤 (3)。注意： 标注有 * 的第 (4) 和 (5) 项是可选要求，视你的进度而定；完成这些挑战将为你获得加分（Merit）。名词定义：BER（误码率）： 错误的信息比特数与传输的总信息比特数之比。SNR（信噪比）： 在加性高斯白噪声（AWGN）信道中，$y = x + n$ 且 $n \sim N(0, \sigma^2)$。$SNR = E_b/N_0$，其中 $E_b$ 是每信息比特能量，$N_0 = 2\sigma^2$。